# 更新紀錄

本專案的版本格式依循 [Semantic Versioning](https://semver.org/lang/zh-TW/)。

---

## [1.2.0] - 2026-08-19

### 新增

- **Apps Script 預熱**：客人進站時在背景送出一次無副作用的 `doGet`，把閒置回收的容器提前喚醒。
  實測同一支只回一行字的函式，冷啟動要 24.7～48.8 秒、熱的只要 1.7 秒——送出等待因此從
  約 20 秒降到約 5 秒。填表期間依互動（聚焦欄位、點商品卡）補打，節流 60 秒避免濫發

### 變更

- `doPost` 移除 `sh.autoResizeRows()`。該呼叫只調整列高、純外觀，卻要多一次試算表往返，
  而客人正在等成功頁。需要整理版面時手動執行 `formatSheet()` 即可

## [1.1.2] - 2026-08-19

### 修正

- `hideProblems()` 只隱藏錯誤總覽、沒有清空清單內容，導致殘留的 `<li>` 可能被誤讀為當前錯誤

### 文件

- `references/deployment.md` 的域名範例改為通用寫法，並補上「域名不要用客戶名」的提醒

## [1.1.1] - 2026-08-19

### 修正

- **重複訂單**：前端送單失敗會自動重試一次，但後端沒有去重機制——`appendRow()` 寫入後
  `sendOrderMail_()` 寄信較慢會拖住回應，前端讀不到就重試，導致同一個訂單編號在試算表出現兩列。
  `doPost` 改為先取 `LockService` 腳本鎖序列化，再依訂單編號檢查是否已寫入，
  已寫入則回 `ok-dup`，不重複寫列也不重複寄信；寫入後加 `SpreadsheetApp.flush()`
  確保資料落地後才釋放鎖

## [1.1.0] - 2026-08-19

### 新增

- **送出失敗總覽**：按下送出但有欄位沒填好時，於表單下方一次列出所有問題（缺哪個欄位、格式哪裡不對），並捲到第一個有問題的欄位、將該欄框線標紅。平常完全隱藏，按下送出才出現，維持版面乾淨
- 表單改用 `novalidate`，驗證改由 `validateOrder()` 統一收集。原生驗證泡泡一次只提示一個欄位且訊息籠統（「請填寫這個欄位」），客人容易誤以為按鈕壞掉
- 新增 `--err` 錯誤色（#c62828），錯誤提示與問題欄位一律使用，與頁面黑白調性區隔

### 修正

- 收件地址、取貨門市、公司、名片等欄位空白時，改為顯示帶正確範例的紅字說明，取代瀏覽器原生泡泡

### 其他

- `scripts/check_site.js` 共 40 項檢查（新增錯誤總覽相關 3 項；地址檢查改為比對實際規則而非 DOM id，重構後不會誤報）

## [1.0.1] - 2026-08-19

### 修正

- **超商取貨要先選品牌**：原本只有一個自由填寫的門市欄位，客人填「711台北濟新」時工作人員分不出是 7-11 還是全家。改成先選 7-11／全家再填店名，送出前把品牌併入「取貨門市」欄位值，Google 試算表欄位與 Apps Script 不需變動
- **宅配地址防呆**：原本只設 `required`，填「台北市」也能送出。改為送出前檢查郵遞區號（3 或 5 碼）、縣市、鄉鎮市區、門牌號碼四項，缺項時明確列出缺什麼並附正確範例

### 其他

- `scripts/check_site.js` 新增 4 項檢查（超商品牌選擇、品牌併入門市欄位、地址防呆、門牌號碼檢查），共 38 項

## [1.0.0] - 2026-08-16

初版發布。

### 新增

- **動態密碼閘門**：每 30 秒更新的 6 位數驗證碼，未通過閘門取不到頁面原始碼；可透過 `GATE_ENABLED` 關閉，改為公開頁
- **商品卡**：多圖輪播、分色選購（每色獨立定價）、數量加減
- **課程／體驗預約**：日曆選場次、每場名額設定，可自動排未來 n 個月的週末場次
- **身份折扣**：專業身份上傳名片後，全站價格自動切換折扣價
- **寄送試算**：宅配／超商兩種運費，材積超限自動鎖住超商選項
- **多階段付款**：百分比自動試算，最後一段吃四捨五入尾差
- **強制閱讀條款**：捲到底才能勾選同意
- **收單雙保險**：HTML 排版通知信（公司＋客人副本）與 Google 試算表訂單總表
- **網站圖示 KV 儲存**：透過 `/fav/<key>` 路由讀取，不 base64 內嵌
- `scripts/check_site.js`：交付前頁面完整性自動檢查
- `scripts/gen_keys.py`：產生 TOTP 金鑰與入場／驗證器 QR
- `references/intake.md`：素材訪談清單
- `references/deployment.md`：部署步驟與歷史教訓
